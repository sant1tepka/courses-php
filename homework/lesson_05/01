<?php

5. () Написать функцию которая на вход получает строку(пароль) и проверяет сложность пароля по шкале Простой, Нормальный, Сложный

function pwd(string $a) {
	if (strlen($a) < 8){
	return "Простой";
	}
	else:
		for
		if (
}

foo("t", 15);

